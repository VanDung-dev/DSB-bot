[project]
name = "dsb-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "discord.py",
    "colorlog",
    "python-dotenv",
    "aiohttp",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
